spring:
  application:
    name: reactive_bootcamp_microservice

  r2dbc:
    url: r2dbc:postgresql://localhost:5434/bootcamp-db
    username: usuario
    password: clave123

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5434/bootcamp-db
    user: usuario
    password: clave123

  main:
    web-application-type: reactive

server:
  port: 8082

capability-service:
  base-url: http://localhost:8081

logging:
  level:
    root: INFO
    org.springframework.r2dbc: DEBUG
    org.springframework.web.reactive.function.client: DEBUG
